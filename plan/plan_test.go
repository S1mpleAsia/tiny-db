package plan_test

import (
	"testing"
)

func TestSingleTablePlan(t *testing.T) {
	// tinyDB, err := server.NewTinyDBWithMetadata(path.Join(".", "single_table_plan_test"))
	// require.NoError(t, err)
}
